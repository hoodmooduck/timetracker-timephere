import "../index.scss";
import Auth from "../Components/AuthFrom/Auth.tsx";

function MainPage() {

    return (
        <main className='auth-page'>
            <Auth />
        </main>
    );
}

export default MainPage;
